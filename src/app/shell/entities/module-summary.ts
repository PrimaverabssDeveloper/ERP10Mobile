import { Module } from '../../core/entities';

/**
 * Contains de summary data for a module.
 *
 * @export
 * @interface ModuleSummary
 */
export interface ModuleSummary {
    module: Module;
    data: any;
}
